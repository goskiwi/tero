# 面试版本最终验证

本报告记录当时的串行配置实验；后续已恢复默认 4 路只读并行，未将历史实验改写为并行成绩。

2026-09-09。默认工具执行设为串行（max_parallel_tools=1），保留显式选择 2～4 路只读并行的能力。
本轮不做并行性能对比，不再扩功能。

## 真实模型结果

模型：DeepSeek V4 Flash，官方 Responses API。场景为订单计价修复＋大结果取回。

- 7 个主模型轮次、10 次工具调用。
- Runtime 最终验证通过，独立目录中的原始验收通过。
- 大诊断命令只运行一次；模型使用 read_artifact 从已保存输出取回 QTY-2718，并写入最终回答。
- 测试、AGENTS.md 和诊断脚本未修改。
- 调用阶段均为 finished；修改前像与任务 Diff 均存在。
- 默认串行，模型仍可在一个回复中提供多个工具调用。

完整数据在 `.tero/evaluations/final-smoke/51ec78f7a2174766/evaluation.json`，同目录保存此次 Session、Trace、Artifact 与交付报告。
这是一项完整执行路径的成功案例，不代表统计成功率。此前核心改动的完整本地回归为 102 项通过；
本轮默认值调整后运行相关 15 项回归，全部通过，并完成本次真实 LLM 验证。

实现范围就此收住：Session 快照、根目录 AGENTS.md、受控本地工具、结果 Artifact、前像和 Diff、
上下文压缩、恢复与失败指引。没有自动回滚、自动 Git 交付或 Code Mode。

本次实际耗时：14.462 秒。
