# Session v7 长任务压缩复测

2026-09-09，DeepSeek V4 Flash，官方 Responses API。

**结果：本次真实长任务与上下文检查通过。**

- 1,000,000 模型窗口、32,000 主输出、24,000 提前压缩阈值。
- 35 个主模型轮次，52 次工具调用，约 360 秒。
- 4 次摘要压缩成功；35 次主请求均保留原始需求，工具调用与结果配对检查通过。
- Runtime 最终验证通过；独立目录使用原始 25 个验收测试再次通过。
- 原测试文件与 AGENTS.md 未改，未确认副作用列表为空。

| 压缩次数 | 历史输入估算：之前 | 之后 |
| --- | ---: | ---: |
| 1 | 23107 | 5662 |
| 2 | 23073 | 12690 |
| 3 | 22918 | 14185 |
| 4 | 23112 | 12918 |

表中是历史输入估算，不含系统规则和工具 Schema；提前触发时计算的是完整输入。因此历史数值可略低于 24K。

后端累计用量：输入 564,420，输出 43,633，输入中缓存命中 347,008；包含摘要调用。这些是整次任务用量，不能当成单次窗口大小，也不能据此宣称压缩降低了某个固定百分比成本。

## 评测脚本问题与范围

本轮第一次尝试发现验收脚本的 for 循环变量被此前编辑误改为 git_args，而使用处仍是 args，导致 NameError。该运行被主动取消并归档为无效验收，不计入模型成败。修正变量名后，从全新工作区重跑；未放宽需求或测试断言。

本报告只证明当前案例的一次通过，不代表所有长任务均稳定，不是完整 1M 输入压力测试，也不等于新增 Runtime 单元用例已全部通过。本轮没有运行完整本地回归套件。

完整证据：`.tero/evaluations/archive/d9f501b647ac456d/evaluation.json`，同目录保留 Session、Trace 与交付报告。
